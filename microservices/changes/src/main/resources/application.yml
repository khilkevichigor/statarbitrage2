server:
  port: 8089

spring:
  application:
    name: changes-service
    
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.changes: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level [CHANGES] %logger{36} - %msg%n"