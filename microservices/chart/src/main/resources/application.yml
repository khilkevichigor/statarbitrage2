server:
  port: 8093

spring:
  application:
    name: chart-service
    
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.chart: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level [CHART] %logger{36} - %msg%n"