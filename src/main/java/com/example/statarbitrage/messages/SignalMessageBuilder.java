package com.example.statarbitrage.messages;

import com.example.statarbitrage.model.PairParameters;
import com.example.statarbitrage.model.Settings;

public final class SignalMessageBuilder {
    private SignalMessageBuilder() {
    }

    public static String buildSignalText(Settings settings, PairParameters pairParameters) {
        StringBuilder sb = new StringBuilder();

        return sb.toString();
    }
}
