package com.example.statarbitrage.messages;

import com.example.statarbitrage.model.PairParameters;
import com.example.statarbitrage.model.StatArbitrageSettings;

public final class SignalMessageBuilder {
    private SignalMessageBuilder() {
    }

    public static String buildSignalText(StatArbitrageSettings settings, PairParameters pairParameters) {
        StringBuilder sb = new StringBuilder();

        return sb.toString();
    }
}
