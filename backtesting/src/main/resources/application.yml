server:
  port: 8092

spring:
  application:
    name: backtesting-service
    
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.backtesting: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level [BACKTESTING] %logger{36} - %msg%n"