server:
  port: 8088

spring:
  application:
    name: python-service
    
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.python: INFO
  pattern:
    console: "%d{HH:mm:ss.SSS} [%thread] %-5level [PYTHON] %logger{36} - %msg%n"